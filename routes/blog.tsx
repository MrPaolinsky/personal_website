import { define } from "../utils.ts";

export default define.page(function Blog(_ctx) {
  return (
    <>
      <section className="about-section">
        <h1 className="main-title">Recent posts</h1>
        <p>There is nothing to see here :c (for now)</p>
      </section>
    </>
  );
});
